# API origin (no trailing slash). Mirrored into NEXT_PUBLIC_API_URL for browser /api/* calls unless you set that explicitly.
BACKEND_API_URL=http://localhost:5000
# NEXT_PUBLIC_API_URL=https://api.example.com
# Must match the backend JWT secret so Next.js middleware can verify Bearer tokens.
JWT_SECRET=68f4ecfa22047347db1b4ac77c3f0e9dc41a4732c39ef4f3614c324b9960f253

# Ticket activity timeline: GET + POST /api/tickets/:id/activity on the same backend (Mongo `ticket_activities`).

# Kanban boards: same BACKEND_API_URL / NEXT_PUBLIC_API_URL as the main API (Express backend sibling repo/folder).
# Board routes live on that server at /api/boards, /api/columns, /api/tasks, etc. No separate boards process.
