# Static site: `next build` writes to `./out`. Set `NEXT_PUBLIC_API_URL` in the Render
# dashboard to your backend origin (same value at build + runtime for `NEXT_PUBLIC_*`).
services:
  - type: web
    name: mokanco-support
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./out
    routes:
      - type: redirect
        source: /onboarding/track/*
        destination: /onboarding/track?token=*
